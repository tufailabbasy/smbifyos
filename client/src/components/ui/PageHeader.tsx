import React from "react";
import { Link } from "react-router-dom";
export interface PageHeaderProps { title: string; badge?: React.ReactNode; description?: string; actions?: React.ReactNode; breadcrumbs?: Array<{ label: string; href?: string }>; }
export function PageHeader({ title, badge, description, actions, breadcrumbs }: PageHeaderProps) { return <div className="flex flex-col gap-4 border-b border-slate-200/80 pb-5 md:flex-row md:items-end md:justify-between"><div className="min-w-0">{breadcrumbs?.length ? <nav aria-label="Breadcrumb" className="mb-2 flex items-center gap-1.5 text-[11px] text-slate-400">{breadcrumbs.map((item, index) => <React.Fragment key={`${item.label}-${index}`}>{index > 0 && <span className="text-slate-300">/</span>}{item.href ? <Link to={item.href} className="hover:text-indigo-600">{item.label}</Link> : <span className="font-medium text-slate-500">{item.label}</span>}</React.Fragment>)}</nav> : null}<div className="flex flex-wrap items-center gap-2.5"><h1 className="text-[25px] font-bold leading-tight tracking-[-.025em] text-slate-950">{title}</h1>{badge}</div>{description && <p className="mt-1.5 max-w-3xl text-[13px] leading-5 text-slate-500">{description}</p>}</div>{actions && <div className="flex shrink-0 flex-wrap items-center gap-2">{actions}</div>}</div>; }