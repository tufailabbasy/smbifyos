import React from "react";
export interface CardProps extends React.HTMLAttributes<HTMLDivElement> { children?: React.ReactNode; }
export function Card({ className = "", children, ...props }: CardProps) { return <div className={["rounded-2xl border border-slate-200/80 bg-white shadow-[0_1px_3px_rgba(15,23,42,.045)] transition-all", className].filter(Boolean).join(" ")} {...props}>{children}</div>; }
export function CardHeader({ title, subtitle, action, className = "" }: { title: React.ReactNode; subtitle?: React.ReactNode; action?: React.ReactNode; className?: string }) { return <div className={["flex flex-col justify-between gap-2 border-b border-slate-100 px-5 py-4 sm:flex-row sm:items-center", className].filter(Boolean).join(" ")}><div className="min-w-0"><h3 className="text-[13px] font-semibold text-slate-900">{title}</h3>{subtitle && <p className="mt-1 text-[11px] leading-5 text-slate-500">{subtitle}</p>}</div>{action && <div className="shrink-0">{action}</div>}</div>; }
export function CardBody({ className = "", children }: { className?: string; children?: React.ReactNode }) { return <div className={["p-5", className].filter(Boolean).join(" ")}>{children}</div>; }
export interface StatCardProps { label: string; value: React.ReactNode; description?: string; icon?: React.ReactNode; color?: "indigo" | "emerald" | "amber" | "rose" | "violet" | "sky"; change?: string; }
const tones = { indigo: "bg-indigo-50 text-indigo-600", emerald: "bg-emerald-50 text-emerald-600", amber: "bg-amber-50 text-amber-600", rose: "bg-rose-50 text-rose-600", violet: "bg-violet-50 text-violet-600", sky: "bg-sky-50 text-sky-600" };
export function StatCard({ label, value, description, icon, color = "indigo", change }: StatCardProps) { return <Card className="p-5"><div className="flex items-start justify-between"><span className="text-[11px] font-semibold uppercase tracking-[.09em] text-slate-500">{label}</span>{icon && <div className={`flex h-9 w-9 items-center justify-center rounded-xl ${tones[color]}`}>{icon}</div>}</div><div className="mt-2 text-[28px] font-bold tracking-tight text-slate-950">{value}</div>{(description || change) && <div className="mt-2 flex items-center gap-2 text-xs text-slate-500">{change && <span className="font-semibold text-emerald-600">{change}</span>}{description && <span>{description}</span>}</div>}</Card>; }