# Workspace Custom Rules

- **Trigger Command `smbify`:**
  Whenever the user inputs a prompt starting with or containing the keyword `smbify`, you must immediately invoke the custom `gmb-optimizer` skill to process the provided Google My Business (GMB) details, screenshots, competitor chats, or Local Falcon data.
