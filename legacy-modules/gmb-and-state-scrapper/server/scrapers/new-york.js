async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("New York scraper not yet configured -- selectors needed");
  // Target URL: https://apps.dos.ny.gov/publicInquiry
  return [];
}

module.exports = {
  scrape,
};
