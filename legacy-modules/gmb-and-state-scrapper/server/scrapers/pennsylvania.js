async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("Pennsylvania scraper not yet configured -- selectors needed");
  // Target URL: https://file.dos.pa.gov/search/business
  return [];
}

module.exports = {
  scrape,
};
