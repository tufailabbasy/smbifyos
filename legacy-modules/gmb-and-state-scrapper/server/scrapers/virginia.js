async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("Virginia scraper not yet configured -- selectors needed");
  // Target URL: https://cis.scc.virginia.gov
  return [];
}

module.exports = {
  scrape,
};
