async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("Arizona scraper not yet configured -- selectors needed");
  // Target URL: https://ecorp.azcc.gov/CommonPages/Corp/CorporationSearch.aspx
  return [];
}

module.exports = {
  scrape,
};
