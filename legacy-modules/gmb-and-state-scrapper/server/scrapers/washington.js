async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("Washington scraper not yet configured -- selectors needed");
  // Target URL: https://ccfs.sos.wa.gov/#/BusinessSearch
  return [];
}

module.exports = {
  scrape,
};
