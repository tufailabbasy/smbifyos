async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("Michigan scraper not yet configured -- selectors needed");
  // Target URL: https://cofs.lara.state.mi.us/SearchApi/Search/Search
  return [];
}

module.exports = {
  scrape,
};
