async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("Illinois scraper not yet configured -- selectors needed");
  // Target URL: https://www.ilsos.gov/corporatellc
  return [];
}

module.exports = {
  scrape,
};
