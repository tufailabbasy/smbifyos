async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("California scraper not yet configured -- selectors needed");
  // Target URL: https://bizfile.sos.ca.gov
  return [];
}

module.exports = {
  scrape,
};
