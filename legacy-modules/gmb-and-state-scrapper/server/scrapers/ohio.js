async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("Ohio scraper not yet configured -- selectors needed");
  // Target URL: https://businesssearch.ohiosos.gov
  return [];
}

module.exports = {
  scrape,
};
