async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("North Carolina scraper not yet configured -- selectors needed");
  // Target URL: https://www.sosnc.gov/online_services/search/by_title/_Business_Registration
  return [];
}

module.exports = {
  scrape,
};
