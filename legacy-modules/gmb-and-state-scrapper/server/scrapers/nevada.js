async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("Nevada scraper not yet configured -- selectors needed");
  // Target URL: https://esos.nv.gov/EntitySearch/OnlineEntitySearch
  return [];
}

module.exports = {
  scrape,
};
