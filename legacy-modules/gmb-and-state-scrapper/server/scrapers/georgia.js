async function scrape({ businessType, limit, onProgress } = {}) {
  void businessType;
  void limit;
  void onProgress;

  console.log("Georgia scraper not yet configured -- selectors needed");
  // Target URL: https://ecorp.sos.ga.gov/BusinessSearch
  return [];
}

module.exports = {
  scrape,
};
